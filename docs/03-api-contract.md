# API Contract

- `GET /api/items`
- `POST /api/items`
- `GET /api/requests`
- `POST /api/requests`
- `PATCH /api/requests/:id/status`

Payloads follow the assessment contract and return friendly errors for invalid input or blocked transitions.
